import type { HealthResponse, PolicyResponse, ReplayConfig } from '../types';

interface Props { config: ReplayConfig; onChange: (config: ReplayConfig) => void; health?: HealthResponse | null; policy?: PolicyResponse | null }

export function ReplayConfigPanel({ config, onChange, health, policy }: Props) {
  const maxConcurrency = policy?.limits.maxConcurrency || health?.policy.maxConcurrency || 1;
  return <section className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5"><h2 className="text-lg font-semibold text-white">Replay controls</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm text-slate-300">Concurrency<input type="number" min={1} max={maxConcurrency} className="mt-1 w-full rounded-lg border border-slate-700 bg-slate-950 p-2" value={config.concurrency} onChange={e => onChange({ ...config, concurrency: Math.min(maxConcurrency, Math.max(1, Number(e.target.value))) })} /></label><label className="text-sm text-slate-300">Delay between requests (ms)<input type="number" min={0} className="mt-1 w-full rounded-lg border border-slate-700 bg-slate-950 p-2" value={config.delayMs} onChange={e => onChange({ ...config, delayMs: Math.max(0, Number(e.target.value)) })} /></label><label className="flex items-center gap-3 rounded-xl border border-slate-800 p-3 text-sm"><input type="checkbox" checked={config.followRedirects} onChange={e => onChange({ ...config, followRedirects: e.target.checked })} /> Follow redirects</label><label className="flex items-center gap-3 rounded-xl border border-slate-800 p-3 text-sm"><input type="checkbox" checked={config.captureBody} onChange={e => onChange({ ...config, captureBody: e.target.checked })} /> Capture body preview</label><label className="flex items-center gap-3 rounded-xl border border-slate-800 p-3 text-sm"><input type="checkbox" checked={config.auditOnly || policy?.auditOnly} disabled={policy?.auditOnly} onChange={e => onChange({ ...config, auditOnly: e.target.checked })} /> Audit-only mode</label></div><p className="mt-3 text-xs text-slate-500">Backend caps concurrency and request count according to active policy.</p></section>;
}
